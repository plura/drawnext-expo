# Always wrap secrets (passwords, API keys, etc.) in quotes if they contain special characters.

# --- Database (DDEV defaults) ---
DB_HOST=db
DB_NAME=db
DB_USER=db
DB_PASS=db

# --- Paths ---
# Absolute path to your project root inside the container/host
PROJECT_ROOT=/var/www/html

# --- Runtime ---
# development | production
ENV=development

# --- Testing helpers ---
TEST_MODE=1
TEST_FILES_DIR=/tests/files

# Optional:
# TIMEZONE=UTC
