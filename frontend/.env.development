# Env vars for Vite (safe to commit)
# Only variables prefixed with VITE_ are exposed to the app.
# If values contain spaces or special characters, wrap them in quotes.

# Environment type (for your own reference)
VITE_ENV=development

# Enable or disable caching of notebooks config in the frontend
# Use "true" in production builds, "false" while developing
VITE_CACHE_ENABLED=false


VITE_ADMIN_DRY_RUN=false


VITE_BACKEND_ORIGIN=https://drawnext-expo.ddev.site
